version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:20.5.1
    steps:
      - checkout
      - run: node --version
      - run: apt-get update && apt-get install -y xvfb
      - run: npm install
      - run: npx cypress --version

workflows:
  version: 2.1 
  build-and-run:
    jobs:
      - build
